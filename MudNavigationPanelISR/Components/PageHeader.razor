@using MudBlazor.Utilities
@inherits MudComponentBase

@if (!string.IsNullOrEmpty(Title))
{
    <PageTitle>@Title</PageTitle>
}

@if (!string.IsNullOrEmpty(Text))
{
    <MudPaper Style="@Stylename" Elevation="@Elevation" >
        <br/>
        <MudText Align="Align.Center" Typo="Typo.h5" GutterBottom=true Color=@TextColor><b>@Text</b></MudText>
        @if (!string.IsNullOrEmpty(SubText))
        {
            <MudText Align="Align.Center" Typo="Typo.body1" Color=@SubtextColor>@SubText</MudText>
        }
    </MudPaper>
    <br/>
}

@code {
    protected string Stylename => new StyleBuilder()
        .AddStyle("background", BackgroundColor, !string.IsNullOrEmpty(BackgroundColor))
        .AddStyle(Style)
        .Build();

    [Parameter]
    public string? Text { get; set; }
    [Parameter]
    public string? SubText { get; set;}
    [Parameter]
    public string? Title { get; set; }
    [Parameter]
    public string? BackgroundColor { get; set; }
    [Parameter]
    public Color TextColor { get; set; } = Color.Primary;
    [Parameter]
    public Color SubtextColor { get; set; } = Color.Inherit;
    [Parameter]
    public int Elevation { get; set;} = 0;
}
